# Opt out of any inherited Yarn PnP from parent directories.
# This repo uses plain npm (node_modules), not Yarn PnP — wrangler's
# bundler otherwise picks up the parent cortex/.pnp.cjs and tries to
# resolve our deps through it.
nodeLinker: node-modules
