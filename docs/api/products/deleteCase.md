# 删除lca

> Auto-generated from `docs/api-raw/2037000724835078144.json`. Anything below `## Integration notes` is hand-edited; the auto-generated section is regenerated by `scripts/render_endpoint_docs.py` — do not edit above the marker.

## Contract

- **Method**: `POST`
- **Path**: `/lca/v3/deleteCase`
- **Service**: LCA3.0
- **Rate limit**: --次/秒
- **apiId**: `2037000724835078144`

## Upstream documentation (raw)

```text
删除lca 接口介绍 根据接口传的caseId删除lca。 接口规则 字段 内容 所属服务 LCA3.0 请求地址 /lca/v3/deleteCase 请求方法 POST 频控策略 --次/秒 请求头 字段名 类型 必填 说明 Content-Type string 是 application/json appId string 是 示例：xxxxx。数据来源：【获取成员key】接口 /“成员key”字段 请求参数 字段 类型 说明 id string lca ID，【查询产品信息】接口lca ID字段 请求示例 curl 'https://open.ecdigit.cn/openapi/lca/v3/deleteCase?id=49132834097266693' -H 'Content-Type: application/json' -H 'appId: XXXXXXXXXX' -d '{}' 响应参数 字段名 类型 说明 详细说明 success boolean 成功标识 true/false code string 状态码 返回200，调用成功 message string 信息 成功/失败 响应示例 { "success": true, "code": "200", "message": "成功" }
```

## Integration notes

### Not smoked

Destructive write — removes an LCA case. Documented from upstream docs
only.

### Contract (from upstream docs)

```
POST https://open.ecdigit.com/openapi/lca/v3/deleteCase
Header: appId: app:xxxxxxxxxxxxxxxxxxx
       Content-Type: application/json
Body:  {"id": "<lca id, from getCaseDetail>"}
```

Same `id` (not `caseId`) parameter convention as `getCaseDetail`. The
docs don't say whether this is a soft-delete (is_deleted flag) or hard
delete; whether calc history is also wiped; or whether deleting a case
auto-deletes the parent product when it becomes empty. **Smoke before
trust.**

### MCP tool mapping

```ts
server.registerTool(
  "delete_case",
  {
    description: "Delete an LCA case. Destructive — once removed, the case's data items, background matches, and calc history are gone (soft- vs hard-delete behaviour to be verified). Skill rule: never call without explicit user confirmation showing the case name, status, and any calc records that will be lost.",
    inputSchema: {
      case_id: z.string().describe("Case id to delete"),
    },
    annotations: { destructiveHint: true, idempotentHint: true },
  },
  async ({ case_id }) => {
    // implementation in src/tools/delete_case.ts
    return { content: [{ type: "text", text: "..." }] };
  },
);
```

### Skill rule

Destructive + idempotent (re-calling on an already-deleted id should
no-op or 404, both acceptable). Always confirm with the user; show the
case name + statusName + calc-record count before submitting. Pattern
match: hiq-editor never auto-deletes anything either.
