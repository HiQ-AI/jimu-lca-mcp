# 创建空间

> Auto-generated from `docs/api-raw/2011014051538276352.json`. Anything below `## Integration notes` is hand-edited; the auto-generated section is regenerated by `scripts/render_endpoint_docs.py` — do not edit above the marker.

## Contract

- **Method**: `POST`
- **Path**: `/lca/v3/addProjectSpace`
- **Service**: LCA3.0
- **Rate limit**: --次/秒
- **apiId**: `2011014051538276352`

## Upstream documentation (raw)

```text
添加项目空间 接口介绍 支持开发者调用接口，创建项目空间。接口支持创建“私有”类型的空间，并且会在创建空间的同时创建一个默认分组。 接口规则 字段 内容 所属服务 LCA3.0 请求地址 /lca/v3/addProjectSpace 请求方法 POST 频控策略 --次/秒 请求头 字段名 类型 必填 说明 Content-Type string 是 application/json appId string 是 示例：xxxxx。数据来源：【获取成员key】接口 /“成员key”字段 请求参数 字段名 类型 必填 说明 name string 否 空间名称 description string 否 空间描述 请求示例 curl 'https://open.ecdigit.cn/openapi/lca/v3/addProjectSpace' -H 'Content-Type: application/json' -H 'appId: XXXXXXXXXX' -d '{ "name":"1111空间", "description":"222222" }' 响应参数 字段名 类型 说明 详细说明 success boolean 成功标识 true/false code string 状态码 返回200，调用成功 message string 信息 成功/失败 data object 返回数据 添加成功的项目空间详情，具体字段见下表 data 对象字段 字段名 类型 说明 详细说明 id long 空间ID 示例：48816880233615365 name string 空间名称 示例：1111空间 dicPermissionId long 空间类型ID 示例：1801221779521712142（私有） description string 空间描述 示例：222222 topCompanyId string 所在更企业id 示例：140 companyId string 所在企业id 示例：140 createId string 创建人ID 示例：2843 createTime string 创建时间 示例：2026-01-14 10:33:41 isDeleted int 是否删除 0-未删除，1-已删除。示例：0 响应示例 { "success": true, "code": "200", "message": "成功", "data": { "id": "48816880233615365", "name": "1111空间", "dicPermissionId": "1801221779521712142", "description": "222222", "topCompanyId": "140", "companyId": "140", "createId": "2843", "createTime": "2026-01-14 10:33:41", "isDeleted": 0 } }
```

## Integration notes

**Wrapped as `create_space` (MCP tool + `jimu-lca create-space` CLI subcommand).**

Originally deferred to the web UI, then re-evaluated (see [non-goals.md](../../architecture/non-goals.md#group-2--space-membership-writes)) when a real Cortex task surfaced: a user needs their own **private** workspace so their modeling doesn't pollute the shared org-public spaces.

Key facts that make this an agent-safe write:

- **Auth = user-level memberKey** (`appId` header), the same credential every other tool uses — *not* the company appkey that the Group 1 bootstrap endpoints need.
- Body is just `name` / `description` (both optional upstream; the wrapper requires `name`).
- The created space is **私有 (private)** by default (`dicPermissionId` = the private permission id) and comes with a default root group — exactly the isolation the user wants.
- No teammate/role writes involved, so it doesn't touch other people's access.

Skill rule: confirm the workspace name with the user before creating — there is **no** delete-space tool (removal is web-UI only).
