# 查询背景数据库版本列表

> Auto-generated from `docs/api-raw/2011773533461549056.json`. Anything below `## Integration notes` is hand-edited; the auto-generated section is regenerated by `scripts/render_endpoint_docs.py` — do not edit above the marker.

## Contract

- **Method**: `GET`
- **Path**: `/lca/v3/getAllocationVersions`
- **Service**: LCA3.0
- **Rate limit**: --次/秒
- **apiId**: `2011773533461549056`

## Upstream documentation (raw)

```text
查询背景数据库版本列表 接口介绍 该接口用于查询所有的背景数据库版本。 接口规则 字段 内容 所属服务 LCA3.0 请求地址 /lca/v3/getAllocationVersions 请求方法 GET 频控策略 --次/秒 请求头 字段名 类型 必填 说明 appId string 是 示例：xxxxx。数据来源：【获取成员key】接口/“成员key”字段 请求示例 curl --location --request GET 'https://open.ecdigit.cn/openapi/lca/v3/getAllocationVersions' -H 'appId: XXXXXXXXXX' 响应参数 字段名 类型 说明 详细说明 success boolean 成功标识 true/false code string 状态码 返回200，调用成功 message string 信息 成功/失败 data list<object> 返回数据 背景数据库版本列表，具体字段见下表 data 数组中的对象字段 字段名 类型 说明 详细说明 id long 版本ID 示例：41523741825687557 version string 版本 示例：Ecoinvent3.10+HiQ1.2 description string 描述 示例：合并了Ecoinvent3.10和HiQ1.2的综合数据库，未做去重处理。 响应示例 { "success": true, "code": "200", "message": "成功", "data": [ { "id": "41523741825687557", "uuid": "613e4ef2-dcc5-400c-935f-aa1820ea7e06", "version": "Ecoinvent3.10+HiQ1.2", "description": "合并了Ecoinvent3.10和HiQ1.2的综合数据库，未做去重处理。", "createTime": "2025-02-26" }, { "id": "41712481948418053", "uuid": "117f438e-19dd-402e-81cb-42c9b055a435", "version": "Ecoinvent3.10", "description": "Ecoinvent3.10版本", "createTime": "2025-03-06" } ] }
```

## Integration notes

### Smoke (2026-05-21, prod)

```
GET https://open.ecdigit.com/openapi/lca/v3/getAllocationVersions
Header: appId: app:xxxxxxxxxxxxxxxxxxx
→ success=true, code=200, 5 background DB versions
```

Observed list:

| `id` | `version` | `uuid` | `description` |
|---|---|---|---|
| 41523741825687557 | Ecoinvent3.10+HiQ1.2 | 613e4ef2-dcc5-... | 合并了 Ecoinvent3.10 和 HiQ1.2 的综合数据库，未做去重处理 |
| 41712481948418053 | Ecoinvent3.10 | 117f438e-19dd-... | Ecoinvent3.10 版本 |
| 47663361112772613 | Ecoinvent3.11 | 9653173e-a03b-... | Ecoinvent3.11 版本 |
| 47684735954251781 | Ecoinvent3.12 | f2ee17eb-dfc1-... | Ecoinvent3.12 版本 |
| 48932530878033925 | Ecoinvent3.12+HiQ1.4.0 | 9a1d4fd6-3231-... | 合并了 Ecoinvent3.12 和 HiQ1.4.0 的综合数据库 |

### Why this matters

- The `id` returned here is the `versionId` parameter that **must** be passed
  to `getAssignedCalculationMethods` (see that endpoint's doc — upstream docs
  do not mention this requirement, but smoke shows it errors with
  `versionId不能为空` if omitted).
- A given memberKey may not have access to all 5 versions in practice — the
  list is filtered by what's "allocated" to this tenant. Don't hardcode any
  versionId on the client side.
- Versions are immutable snapshots — `Ecoinvent3.12+HiQ1.4.0` is a different
  resource from `Ecoinvent3.10+HiQ1.2`, not a newer "version" in the
  package-version sense. Pick the right one per LCA case.

### Response shape (verified)

Matches upstream doc exactly, including `createTime`. No quirks observed.

### MCP tool mapping

```ts
server.registerTool(
  "list_background_db_versions",
  {
    description: "List background-database versions this memberKey can use (Ecoinvent 3.10 / 3.11 / 3.12 / HiQ-combined variants). Each `id` is a `versionId` that calculation-method lookup, calc submission, and most downstream LCA operations require. Pick a version once per LCA case; mixing versions inside one case is not supported.",
    inputSchema: {

    },
    annotations: { readOnlyHint: true },
  },
  async (_args) => {
    // implementation in src/tools/list_background_db_versions.ts
    return { content: [{ type: "text", text: "..." }] };
  },
);
```

Same caching hint as `getAllUnits` — reference data, cache 1 hour at module
level.
