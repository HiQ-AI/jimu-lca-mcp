# 添加成员

> Auto-generated from `docs/api-raw/2011985039088291840.json`. Anything below `## Integration notes` is hand-edited; the auto-generated section is regenerated by `scripts/render_endpoint_docs.py` — do not edit above the marker.

## Contract

- **Method**: `POST`
- **Path**: `/lca/v3/addMemberToSpaceBatch`
- **Service**: LCA3.0
- **Rate limit**: --次/秒
- **apiId**: `2011985039088291840`

## Upstream documentation (raw)

```text
批量添加成员到项目空间 接口介绍 该接口用于批量添加成员到项目空间，通过POST请求接收参数并调用业务层处理。 备注：只支持在自己私有空间下添加成员。若用户已经是空间内成员，请勿重复添加。 接口规则 字段 内容 所属服务 LCA3.0 请求地址 /lca/v3/addMemberToSpaceBatch 请求方法 POST 频控策略 --次/秒 请求头 字段名 类型 必填 说明 Content-Type string 是 application/json appId string 是 示例：xxxxx。数据来源：【获取成员key】接口 /“成员key”字段 请求参数 字段名 类型 必填 说明 spaceId Long 是 空间ID。示例：42660195039043589。数据来源：【查询空间列表】接口 / “空间ID”字段 userIds List<Long> 是 用户ID集合。示例：[39915158705491973]。数据来源：【查询用户列表】接口 / “用户ID”字段。 请求示例 curl 'https://open.ecdigit.cn/openapi/lca/v3/addMemberToSpaceBatch' -H 'Content-Type: application/json' -H 'appId: XXXXXXXXXX' -d '{ "spaceId": "42660195039043589", "userIds": [ "39915158705491973" ] }' 响应参数 字段名 类型 说明 详细说明 success boolean 成功标识 true/false code string 状态码 返回200，调用成功 message string 信息 成功/失败 data boolean 返回数据 操作结果，true表示添加成功 响应示例 { "success": true, "code": "200", "message": "成功", "data": true }
```

## Integration notes

**Not wrapped (see [non-goals.md](../../architecture/non-goals.md)).**

Adding teammates to a workspace is governance, not an agent task. Same endpoint also handles role updates.

The contract above stays in this file as a reference catalog entry —
useful if a Cortex use case ever forces re-evaluating the exclusion
(see the "What changes this list" section of non-goals.md). For now,
this endpoint has no MCP tool and no companion CLI subcommand.
