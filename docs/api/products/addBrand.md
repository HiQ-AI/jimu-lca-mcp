# 创建产品

> Auto-generated from `docs/api-raw/2011335695137902592.json`. Anything below `## Integration notes` is hand-edited; the auto-generated section is regenerated by `scripts/render_endpoint_docs.py` — do not edit above the marker.

## Contract

- **Method**: `POST`
- **Path**: `/lca/v3/addBrand`
- **Service**: LCA3.0
- **Rate limit**: --次/秒
- **apiId**: `2011335695137902592`

## Upstream documentation (raw)

```text
创建产品 接口介绍 支持开发者调用接口，使用模型创建产品，创建产品的同时会自动生成一个lca。创建产品需要在指定的空间、分组下创建，行业、分类、模型等数据信息来自于【查询模型列表】，背景数据库版本id来自于【查询背景数据库版本列表】。 备注：只可以在自己有权限空间下创建产品，有权限的空间可以从【查询空间列表】获取。 接口规则 字段 内容 所属服务 LCA3.0 请求地址 /lca/v3/addBrand 请求方法 POST 频控策略 --次/秒 请求头 字段名 类型 必填 说明 Content-Type string 是 application/json appId string 是 示例：xxxxx。数据来源：【获取成员key】接口 /“成员key”字段 请求参数 字段名 类型 必填 说明 name string 是 产品名称 description string 否 产品描述 industryId Long 是 行业ID。数据来源：【查询模型列表】接口 / “行业ID”字段 categoryId Long 是 分类ID。数据来源：【查询模型列表】接口 / “分类ID”字段 modelId Long 是 模型ID。数据来源：【查询模型列表】接口 / “模型ID”字段 spaceId Long 是 空间ID。数据来源：【查询空间列表】接口 / “空间ID”字段 groupId Long 是 分组ID。数据来源：【查询空间列表】接口 / “分组ID”字段 modelFlag string 是 模型类型，"own"：我的模型；"EC"：易碳模型 versionId Long 是 背景数据库版本ID。数据来源：【查询背景数据库版本列表】接口 / “版本id”字段 modelType Long 是 建模方式。流程：41524144274399259；离散：41524144274399258 calculateDimensions Long 是 计算维度。仅计算LCA：41524144274399269；同时计算LCA和LCC：41524144274399370 请求示例 curl 'https://open.ecdigit.cn/openapi/lca/v3/addBrand' -H 'Content-Type: application/json' -H 'appId: XXXXXXXXXX' -d '{ "name": "aaaa产品", "industryId": "58907799840319887", "categoryId": "16358793212735261", "description": "ces", "modelId": "48839611173421061", "spaceId": "42660195039043589", "groupId": "42660195040092165", "versionId": "41712481948418053", "modelFlag": "own" }' 响应参数 字段名 类型 说明 详细说明 success boolean 成功标识 true/false code string 状态码 返回200，调用成功 message string 信息 成功/失败 data object 返回数据 创建的产品案例信息，具体字段见下表 data 对象字段 字段名 类型 说明 详细说明 id long 案例ID 示例：48840167675604997 uuid string 案例UUID 示例：dcdfcbd0-cba7-47d9-acfd-44449363631d brandId long 产品ID 示例：48840167672721413 reportDate string 报告期 示例：2024-01~2024-12 boundaryId long 边界范围ID 示例：1519532547259908121 unitId long 单位ID 示例：41639498723282949 unitComment string 单位描述 示例：1立方米EPE珍珠棉 consultProductName string 参考产品名称 示例：EPE珍珠棉 consultProductCoefficient decimal 转换系数 示例：30 status string 状态 示例：15889436508619985 createId long 创建人ID 示例：2843 createTime string 创建时间 示例：2026-01-15 11:14:16 description string 描述 示例："" orderNum int 序号 示例：1 unitType long 单位类型ID 示例：1621385735695621126 versionId long 版本ID 示例：41712481948418053 updateTime string 更新时间 示例：2026-01-15 11:14:16 enableBackData int 启用背景数据标志 0-未启用，1-启用。示例：0 modelType long 建模方式 示例：41524144274399259 calculateDimensions long 计算维度 示例：41524144274399269 响应示例 { "success": true, "code": "200", "message": "成功", "data": { "id": "48840167675604997", "uuid": "dcdfcbd0-cba7-47d9-acfd-44449363631d", "brandId": "48840167672721413", "reportDate": "2024-01~2024-12", "boundaryId": "1519532547259908121", "unitId": "41639498723282949", "unitComment": "1立方米EPE珍珠棉", "consultProductName": "EPE珍珠棉", "consultProductCoefficient": "30", "status": "15889436508619985", "createId": "2843", "createTime": "2026-01-15 11:14:16", "description": "", "orderNum": 1, "unitType": "1621385735695621126", "versionId": "41712481948418053", "updateTime": "2026-01-15 11:14:16", "enableBackData": 0, "modelType": "41524144274399259", "calculateDimensions": "41524144274399269" } }
```

## Integration notes

### Not smoked

Write endpoint that creates a new product (brand) in the tenant.
Documented from upstream docs only; will be validated against a
sandbox member before going live.

### Contract (from upstream docs)

```
POST https://open.ecdigit.com/openapi/lca/v3/addBrand
Header: appId: app:xxxxxxxxxxxxxxxxxxx
       Content-Type: application/json
Body:  {
  "name": "<product display name>",
  "description": "<optional>",
  "industryId": "<from list_models — pick the industry the product is in>",
  "categoryId": "<from list_models — pick the matching product category>",
  "modelId": "<from list_models — template the new product starts from>",
  "spaceId": "<from list_spaces — where to file the product>",
  "groupId": "<from list_spaces — sub-folder inside the space>"
}
```

### Why the input is "5 ids + a name"

A new product in 积木LCA 3.0 is not a blank slate — it's instantiated
from a **model template** (`modelId`) and inherits that template's
stage / process / data-item structure. The (industryId, categoryId)
pair must be consistent with the model. The agent's job is to surface
the right `modelId` choice; the rest are scoping.

### Aggregator-style wrapper

The MCP wrapper takes friendly inputs and resolves the 5 ids server-side
when possible:

```ts
server.registerTool(
  "create_product",
  {
    description: "Create a new product (brand) by instantiating a model template. The wrapper looks up industryId / categoryId from the chosen model (they must match) and defaults groupId to the space's root group when omitted, so the agent only has to provide the four semantic inputs (name, model, space, optional group).",
    inputSchema: {
      name: z.string().describe("Product display name (must be unique-ish in the tenant)"),
      model_id: z.string().describe("Model template id, from list_models"),
      space_id: z.string().describe("Space id, from list_spaces"),
      group_id: z.string().describe("Optional group/folder id. Defaults to the space's root group if omitted.").optional(),
      description: z.string().describe("Optional product description").optional(),
    },
    annotations: { destructiveHint: false, idempotentHint: false },
  },
  async ({ name, model_id, space_id, group_id, description }) => {
    // implementation in src/tools/create_product.ts
    return { content: [{ type: "text", text: "..." }] };
  },
);
```

### Open questions

- **Idempotency** — does a duplicate name in the same space error or
  succeed silently? Validate before assuming.
- **Default group** — does omitting `groupId` actually work, or is the
  doc's "required" annotation strict? May need the wrapper to fetch the
  space's root group explicitly.
- **Returns the new brandId?** Doc snippet truncated; need to smoke to
  confirm response shape, which determines whether the wrapper can
  return a usable id without a follow-up call.

### Skill rule

Creating a product is a structural side effect. Skill requires the
agent to show the user:

- Resolved model name (not just id)
- Target space + group display path
- Product name

…then wait for confirmation. Same gate hiq-editor uses for
`create_process`.
