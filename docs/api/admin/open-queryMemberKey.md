# 获取成员key

> Auto-generated from `docs/api-raw/2018918321059139584.json`. Anything below `## Integration notes` is hand-edited; the auto-generated section is regenerated by `scripts/render_endpoint_docs.py` — do not edit above the marker.

## Contract

- **Method**: `POST`
- **Path**: `/open/queryMemberKey`
- **Service**: 账号管理
- **Rate limit**: --次/秒
- **apiId**: `2018918321059139584`

## Upstream documentation (raw)

```text
获取成员key 接口介绍 支持开发者调用接口，获取成员key。通过成员的用户名（name）和手机号（mobile）查询成员key。成员key用于cbam、lca等业务接口的调用。 接口规则 字段 内容 所属服务 账号管理 请求地址 /open/queryMemberKey 请求方法 POST 频控策略 --次/秒 请求头 字段名 类型 必填 说明 Content-Type string 是 application/json 请求参数 字段名 类型 必填 说明 orgSign string 是 平台标识，在开放平台控制台，创建应用时，自定义的标识。示例：liuyanlanYY name string 是 用户名，不可重复。示例：000001 mobile string 是 用户唯一标识联系人电话，不可重复。示例：19811111114 请求示例 curl 'https://open.ecdigit.cn/openapi/open/queryMemberKey' -H 'Content-Type: application/json' -d '{ "name": "000001", "mobile": "19811111114", "orgSign": "liuyanlanYY" }' 响应参数 字段名 类型 说明 详细说明 success boolean 成功标识 true/false code string 状态码 返回200，调用成功 message string 信息 成功/失败 data string 返回数据 成员key，示例：app:1864566588615823360 响应示例 { "success": true, "code": "200", "message": "成功", "data": "app:1864566588615823360" } 错误码/报错信息 错误码 报错信息 说明 平台标识必传! 该平台标识未创建 未找到对应公司,请联系管理员 用户名不能为空! 校验平台标识 手机号不能为空 未找到对应的成员 orgSign对应的企业下找不到用户名和手机号对应的成员
```

## Integration notes

**Not wrapped (see [non-goals.md](../../architecture/non-goals.md)).**

Convenience for a sysadmin to look up an existing member's key by `(orgSign, name, mobile)`. The agent does not discover member keys — its memberKey is handed in via env. Useful in the future CLI bootstrap subcommand for ops scripts.

The contract above stays in this file as a reference catalog entry —
useful if a Cortex use case ever forces re-evaluating the exclusion
(see the "What changes this list" section of non-goals.md). For now,
this endpoint has no MCP tool and no companion CLI subcommand.
